server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/clientedb
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  h2:
    console:
      enabled: true

cliente:
  service:
    url: http://localhost:8080/api/clientes
